@page
@model TestePerformance2.Pages.CityManager.CityListModel
@{
	ViewData["Title"] = "Lista de cidades";
}

<h1>@ViewData["Title"]</h1>

<ul>
	@foreach (var city in Model.Cities)
	{
	<li>
		<a asp-page="CityDetails" asp-route-cityName="@city">Ver detalhes de @city</a>
	</li>
	}
</ul>